export interface ServiceItem {
  id: string;
  title: string;
  description: string;
  tag: string;
}

export interface StepItem {
  stepNum: string;
  title: string;
  description: string;
  id: string;
}

export interface OrderFormData {
  name: string;
  phone: string;
  requirements: string;
}

